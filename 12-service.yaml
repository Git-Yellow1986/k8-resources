---
apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  ports:
    - name: nginx-port
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    environment: dev
    module: backend
    project: expense
# apiVersion: v1  
# kind: Service
# metadata:
#   name: nginx
# spec:
#   selector:
#     project: expense
#     module: backend
#     environment: dev
#   ports:
#   - name: nginx-svc-ports
#     protocol: TCP
#     port: 80 # service port
#     targetPort: 80 # container port